# _on_retry_pressed()

- get_tree().paused = false
> Stop the game scene.

- load_level(LevelsManager.current_level)
> Call the load_level function.

- ball.reset_to_spawn()
> Call the ball.gd reset_to_spawn function.
